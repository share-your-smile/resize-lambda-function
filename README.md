# resize-lambda-function

## usage

- install `sharp`

```bin/bash
# linux
npm install sharp

# Mac
npm install --arch=x64 --platform=linux --target=12.13.0  sharp
```

- packaging

```
zip -r function.zip .
```